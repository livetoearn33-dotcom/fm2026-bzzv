# PM 作戰板

目標只有一個：**9/6（日）10:00 前交出「≤2 分鐘影片＋介紹文字＋開源 GitHub」**。
Round 1 沒有現場 demo，評審只看這三樣。所以一切工作的終點是影片，不是 App。

最後更新：9/2（三）。負責維護：Zeno（每個 sync 點後更新「現況」欄）。

---

## 0. 先給 Zeno 一顆定心丸

**今天 repo 裡已經有能剪出影片的東西。**

design/refined.html 是離線可跑、有真互動的完整 UI（代寫 5 狀態 × 3 語氣、防自爆 2 狀態，自帶手機外框＋LINE 風格宿主畫面）。prompts/ 裡 demo 兩幕的台詞（golden path）也都寫好了。

意思是：就算 Vincent、Brian、Zanna 三個人全部開天窗，你自己用瀏覽器全螢幕錄 refined.html、照 demo-script 配口白，也能交出一支合格的 Round 1 影片。

**所以這份作戰板的邏輯是：底線已保，接下來每一項工作都是在「往上加真實度」。任何一塊拼不上，就退回上一層真實度，不會歸零。**

---

## 1. 整合地圖：誰的東西交給誰、什麼順序拼

### 每個人的產出（artifact）流向

| 誰 | 產出什麼 | 交給誰 | 對方拿去做什麼 |
|---|---|---|---|
| Zeno | prompts/（三引擎＋語氣＋範例＋角色卡）、data/（facts、contacts） | **Brian** | 後端照 prompts/README-組裝說明.md 讀檔組 system prompt |
| Zeno | docs/demo-script.md（demo 敘事） | **全隊** | 所有畫面、台詞、剪接的唯一依據 |
| Brian | 後端三支 API：/analyze、/guard、/persona（含 golden path 快取） | **Zanna 的前端** | HTML 的 JS 用 fetch 打這三支，拿 JSON 畫狀態 |
| Zanna | 兩個情境的 HTML 全狀態（基底＝refined.html）＋簡報視覺＋片頭字卡 | **Vincent**、**Zeno** | Vincent 把 HTML 放進 WebView assets；Zeno 拿去錄影、做簡報 |
| Vincent | Android 殼：Accessibility 讀對話、WebView 容器、JS bridge、ACTION_SET_TEXT 填回 | **Zeno** | 真機端到端錄影（影片的最高真實度版本） |
| Zeno | 影片（9/5 晚錄、9/6 凌晨剪）＋介紹文字 | **主辦** | 9/6 10:00 前提交 |

### 誰等誰（關鍵：前三天大家其實互不相等）

```
9/4–9/5 上午：三條線完全平行，誰都不用等誰
├─ Brian：契約在 prompts/README-組裝說明.md，測資在 spec.md 的 JSON 範例，直接開工
├─ Zanna：所有狀態先用 spec.md 裡的範例 JSON 當假資料畫，不用等後端
└─ Vincent：修 build、做浮層入口、解 Nekogram 寫死，全部不依賴另外兩人

拼裝只有三步，全部集中在 9/5：
① HTML 接真 API（Zanna × Brian）——在「電腦瀏覽器」就能拼，不需要 Android
② HTML 裝進 WebView（Zanna × Vincent）——bridge 傳文字進、填字出
③ 真機端到端（三人＋Zeno 驗收）——只求 demo 兩幕在一台手機上走通一次
```

**白話版**：①②可以同時做、互不擋路。③一定要等①②都好。如果 9/5 下午③還拼不起來，就不硬拼——影片改用①的成果（瀏覽器版）錄，一樣能交。

### 9/4 開場（11:00）必須拍板的三件事，不拍板誰都動不了

| # | 分歧 | A 案 | B 案 | 預設建議 |
|---|---|---|---|---|
| 1 | 讀對話的方式 | 照 spec：Accessibility 直接拿**文字節點**，組成 JSON 給 /analyze（快、準、省 token） | 照 Vincent 現況：**截圖**，後端要多接 VLM 看圖（慢、貴、Brian 工作量翻倍） | **A**。Brian 進度零，不能再給他加 VLM；敘事上照樣可以說「它看得到螢幕」 |
| 2 | 第一幕的介面形態 | 照 refined.html：兩幕都用**鍵盤上方面板**（已做好） | 照 spec 定案（8/31）：第一幕另做一個**浮層大卡片** surface | **A**。省掉一整個 surface 的工，demo 張力在狀態轉換不在版面形態。注意：選 A 等於推翻 spec 8/31 的定案，拍板後要回頭改 spec.md 的定案表，兩份文件才不會打架 |
| 3 | demo 鎖定哪個宿主 App 錄影 | LINE（觀眾最有感） | Nekogram（Vincent 已通） | 先試 A，9/4 晚上 Vincent 試不過就鎖 B。**錄影只需要一個 App 真的通** |

拍板人是 Zeno。開場 30 分鐘內拍完，拍完各自散開寫程式。

---

## 2. 現況盤點（9/2 實際查 git，不是印象）

### main 分支上「已經有」的

| 東西 | 位置 | 狀態 |
|---|---|---|
| Prompt 三引擎＋語氣＋範例＋三張角色卡 | prompts/（8 個 .md） | ✅ 完成，含組裝契約與 golden path 規則 |
| 知識庫 | data/facts.json、data/contacts.json | ✅ 完成（擬真合成資料，可開源） |
| Demo 腳本、spec、視覺方向包 | docs/ | ✅ 完成，含 API 契約的 request/response 範例 |
| **精修 UI 原型** | design/refined.html（573 行，純 HTML/CSS/JS，字型本機打包、零外部請求） | ✅ 可互動、離線可跑 |

### refined.html 對 Zanna 交付物的覆蓋率：核心畫面約六成，整體約五成

已覆蓋（demo 最重要的兩格大致都有骨架）：

- 代寫情境 5 狀態：待命 → 讀取中（掃光動畫）→ 有建議 → 展開（含情境判讀＋理由＋換一版）→ 採用後
- 三種語氣切換：一般／高壓／敏感（高壓局的「他已經問第二次」判讀正是第一幕要的）
- 防自爆情境 2 狀態：收合 56dp／展開，含錯字與語氣風險的文字標記（對應 /guard 的 spans）
- 設計語言整套定案（Raycast 系、配色字級間距都有了）——這是最難的部分，已經做完

還缺的（＝Zanna 9/4–9/5 的實際工作清單，按 demo 重要性排序）：

1. **safeCard → reply「接續生長」動畫**（第一幕的靈魂：文字要像長出來，不是換掉）
2. **來源標註小標籤**（「引用：2026 報價表 v3」——最便宜的可信度證明，評審有兩位技術背景）
3. **對比展開**（「一般 AI 會回什麼」vs 我們的回覆）
4. 防自爆「採用後收回」狀態
5. 把展示頁**抽成可嵌 WebView 的元件版**（拿掉手機外框與裝飾鍵盤，接受 bridge 傳入的真實文字）
6. 跨 App 一幕（動畫或預錄即可）、知識庫設定靜態圖（被問到才開，最低優先）
7. 簡報視覺＋片頭字卡（Round 2 才用得到簡報；片頭字卡 9/5 剪片要用）

### feat/android-shell 分支（Vincent，未合併進 main）

- EQCopilot/ Android 專案：EQAccessibilityService.kt（1040 行主體）＋ MainActivity ＋ NotificationListener，WebView 已能載本機 HTML 測試頁，ACTION_SET_TEXT 已通
- 已知問題（Vincent 自報）：build 有壞檔、寫死只認 Nekogram、浮層入口未做、IME 未做
- 分支裡沒有任何對外網路呼叫——還沒接後端，符合預期

### 確認為零的

- **後端：0%。** 全 repo 沒有任何 server 程式碼。/analyze、/guard、/persona 目前只存在於 prompt 契約文件裡。Brian 是從白紙開工——但契約、測資、golden path 規則全部備好了，這是「照文件施工」不是「邊想邊做」。

### 場外待辦

- 主辦剩一題未答：**賽前寫好的 codebase 能不能用**（TG 追問中）。9/4 開賽前要有答案；若答「不能」，現有產出全部降為「參考稿」，開賽後重打——這是核彈級變數，Zeno 每天追一次
- 提交前要把 feat/android-shell 合併回 main（開源 repo 必須含程式碼）

---

## 3. 風險排序（每條都有：最晚何時解、解不了怎麼降級）

| # | 風險 | 為什麼排這裡 | 最晚何時要解 | 降級方案（解不了就啟動，不猶豫） |
|---|---|---|---|---|
| 1 | **Zanna 進度不明** | 影片畫面全靠 UI；她的狀態是唯一「連進度都不知道」的 | **9/4 18:00 前要看到她的第一個 commit**（或明說做不了） | **refined.html 直接當產品 UI。** 缺的四樣動態（生長動畫、來源標籤、對比展開、採用收回）由 Zeno 帶 AI 工具在 refined.html 上補——它是純 HTML，改得動。Zanna 轉去只做簡報＋片頭字卡（工作量小、不擋錄影） |
| 2 | **Brian 後端零起步** | 唯一從零開始的模組；他還兼「整合」，自己遲到會拖累拼裝 | **9/5 12:00 前 /analyze 的 golden path 要能 curl 通**（demo 那句固定訊息 → 回 spec 範例那包 JSON） | 分兩段降：① 只做 golden path 靜態回包（不接 LLM），demo 兩幕照樣真互動；② 連 server 都沒有 → 範例 JSON 直接寫死進前端 JS，影片照錄，評審看不出差別。/persona 與 /guard 的降級同理（golden path 都已寫在 prompts/） |
| 3 | **契約分歧沒拍板**（截圖 vs 文字、一個面板 vs 兩個 surface） | 分歧每多活一天，就有人在做白工 | **9/4 11:00 開場會，當場拍死** | 拍不定就照本作戰板第 1 節的預設建議走（文字節點＋單一面板形態）。PM 原則：錯的決定好過沒決定，9/4 還能改，9/5 不能 |
| 4 | **Vincent 的殼修不完**（build 壞、只認 Nekogram、浮層入口） | 影響的是「真機錄影」這個最高真實度層，不影響能不能交件 | build **9/4 18:00** 要能一鍵跑；**9/5 15:00** 真機要走通一次 demo 第一幕 | 逐層退：① 只認 Nekogram → 就用 Nekogram 錄，口播不提 App 名；② WebView 拼不進 → 影片用電腦瀏覽器錄 refined.html（自帶手機外框，畫面一樣像手機）；③ ACTION_SET_TEXT 那段已通，單獨錄一顆「真機填字」鏡頭剪進去，證明真實性 |
| 5 | **影片與提交件本身** | 9/6 10:00 交的是影片＋文字＋repo，不是 App；剪片時間最容易被「再修一下功能」吃掉 | 凍結 **9/5 20:00**、錄影 21:00、粗剪 **9/6 02:00**、提交 **09:30** | 凍結後任何人說「再給我半小時修 bug」一律拒絕——影片拍現在有的。介紹文字 9/5 白天先寫好草稿；提交目標 09:30，留 30 分鐘緩衝給上傳出包 |
| 6 | 賽前 codebase 能不能用（主辦未答） | 機率低、殺傷力大 | 9/4 開賽前 | 若不能用：現有 repo 轉為「規格與參考」，開賽後以它為藍圖重打（prompt 與 HTML 重打很快，這正是所有東西都文件化的原因） |

---

## 4. 檢查點表：9/4 11:00 → 9/6 10:00

規則：每個 sync 點 15 分鐘站著開，只對表、不討論實作。表裡「必須存在」的東西不在，**當場宣布啟動對應降級**，不延到下一個 sync。

| 時間 | 這個時間點必須存在什麼 | 沒有的話，啟動什麼 |
|---|---|---|
| **9/4（五）11:00** 開場 | 三個拍板拍完（讀對話方式／介面形態／宿主 App）；Zanna 當面確認「以 refined.html 為基底」還是重畫（重畫需 Zeno 批准，預設不准）；三人各自講出「今晚 23:00 我會交出什麼」 | 拍板僵住 → 照第 1 節預設建議執行，Zeno 一句話定案 |
| **9/4 18:00** | Vincent：build 能一鍵裝上手機。Brian：專案跑得起來、/analyze 有殼（回什麼都行）。Zanna：第一個 commit（生長動畫或來源標籤其一） | Zanna 無 commit 且說不清 → **風險 1 降級啟動**：宣布 refined.html 就是產品 UI，她改做簡報。Vincent build 修不好 → 當晚 Brian 或 Zeno 陪修一小時，仍不行則真機線降級預備 |
| **9/4 23:00** | Brian：/analyze golden path 本機 curl 得通。Zanna（或接手的 Zeno）：生長動畫能看。Vincent：WebView 載入我們的 HTML 成功（bridge 可以還沒通） | 個別缺 → 不熬夜硬追，明早 sync 對照風險表決定；記住每一條都有降級，沒有「必須今晚」 |
| **9/5（六）09:00** | 拼裝①開工：HTML 在瀏覽器裡打真 API 跑通第一幕。拼裝②開工：bridge 介面名三人對齊 | ①卡住 → Brian 全力只保 /analyze golden path，其他 API 先放 |
| **9/5 12:00** ⚠️ 中午線 | **①必須完成**：瀏覽器裡點一下 → safeCard 秒出 → 回覆長出來 → 來源標籤，全程真 API。這是影片的保底畫面 | **後端還不通 → 風險 2 第②段降級：JSON 寫死進前端，下午照常錄影準備，Brian 轉去繼續修、修好算加分。UI 動態還缺 → Zeno 下午親自帶 AI 補，Zanna 手上東西全部收走** |
| **9/5 15:00** | 拼裝③驗收：真機上 demo 第一幕走通一次（哪怕只在一個 App、只走 golden path） | 走不通 → **真機線關閉**，影片確定用瀏覽器版錄；Vincent 只交「ACTION_SET_TEXT 真機填字」單顆鏡頭 |
| **9/5 18:00** | 錄影腳本逐鏡確認（照 demo-script 2:00 影片版）；片頭字卡到位；介紹文字草稿完成 | 字卡沒有 → 純文字卡替代，10 分鐘能做 |
| **9/5 20:00** 🧊 | **功能凍結。** 之後只准修「錄影會拍到的 bug」 | 有人要求延後凍結 → 拒絕。拍現在有的 |
| **9/5 21:00–23:00** | 錄影完成：主片段＋備用 take 各至少一次；素材當場檢查有沒有錄到 | 錄不完 → 砍鏡頭不砍睡眠上限：跨 App 一幕最先砍（本來就「可以演」） |
| **9/6（日）02:00** | 影片粗剪完成（2:00 版），全隊看一次 | 剪不完 → 砍到 1:40 也比遲交好；對比展開可用靜態畫面替代動態 |
| **9/6 08:00** | 提交包齊：影片成品、介紹文字定稿、feat/android-shell 已合併進 main、repo 公開設定確認 | 合併有衝突 → 不解衝突，把 EQCopilot/ 整包複製進 main 提交（開源要的是程式碼在，不是 git 歷史漂亮） |
| **9/6 09:30** | **按下提交。** 不等 10:00 | — |

### 回答驗收題：「9/5 中午如果 X 還沒好，我該做什麼」

- 後端沒好 → JSON 寫死進前端，影片照錄（評審看不出差別，這不是作弊，demo-script 的真實度分層本來就允許）
- UI 動態沒好 → Zeno 收走親自補，Zanna 只剩簡報字卡
- Android 沒好 → 再給 3 小時到 15:00，然後果斷關閉真機線、瀏覽器錄影
- **三個都沒好 → 第 0 節的底線：refined.html ＋ golden path 台詞，今天就能錄。你不會沒有東西交。**

---

## 5. Zeno 的每日三問（直接複製貼上，每天問一輪）

問完對照上表。答案含糊＝紅燈，追問到具體為止；「差不多了」「應該可以」都算含糊。

### 問 Vincent

1. 「build 現在能不能在你電腦上一鍵裝到手機？不能的話，卡住的是哪個檔案？」
2. 「WebView 載我們 repo 的 HTML 了嗎？bridge 這條——對話文字傳進去、ACTION_SET_TEXT 填回來——現在通到哪一步？」
3. 「LINE 上試過了沒？如果明天中午前試不過，demo 就鎖定用 Nekogram 錄，你 OK 嗎？」

### 問 Brian

1. 「/analyze 現在用 curl 打，demo 那句『這個進度到底怎麼樣了？下午要跟客戶開會。』回得出 spec 裡那包 JSON 嗎？」（注意：golden path 是**全文逐字比對**，測試時整句照抄，少一個字就會落到真 LLM，別誤判成後端壞掉）
2. 「組裝說明的四層 prompt，程式現在讀到第幾層？golden path 的完全比對做了沒？」
3. 「如果明天中午前只來得及保一支 API，你保 /analyze 對吧？/guard 跟 /persona 你排在什麼時候？」

### 問 Zanna

1. 「你是在 refined.html 上面改，還是在做新的？（如果是新的：停，先來找我談）」
2. 「缺的四樣動態——文字生長、來源標籤、對比展開、採用收回——今天能交出哪幾樣？最有把握的先做哪個？」
3. 「把你現在的畫面用手機瀏覽器開起來，截圖丟群組給我看一眼。」

### Zeno 自己的每日三件事

1. 追主辦：「賽前 codebase 能不能用」有沒有回（回了立刻通知全隊）
2. 對照檢查點表：今天該存在的東西存在了嗎？不存在，當場宣布降級，不心軟
3. 影片是主戰場：每天花 30 分鐘推進「跟影片直接相關」的東西（腳本逐鏡表、介紹文字、口播練習），不要全部堆到 9/5 晚上

---

## 附：一張圖記住整個作戰

```
底線（已存在）      refined.html ＋ golden path 台詞 → 瀏覽器錄影 ✅ 今天就能交
  ↑ 加真實度       ＋ Brian 的真 API（瀏覽器裡跑通）         ← 9/5 12:00 中午線
  ↑ 加真實度       ＋ 裝進 Vincent 的 WebView、真機走通       ← 9/5 15:00
天花板             真機跨 App 端到端                          ← 有就錦上添花，沒有不追
```

每往上一層，影片更有說服力；任何一層拼不上，退回下一層就好。**唯一不可退的只有 9/6 09:30 按下提交。**
